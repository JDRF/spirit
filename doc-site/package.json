{
  "name": "spirit-doc-site",
  "description": "",
  "main": "index.js",
  "scripts": {
    "link-local-library": "cd ../library && npm install && gulp build:all && npm link && cd ../doc-site && npm link spirit",
    "unlink-local-library": "npm unlink spirit --no-save && npm install",
    "prepack": "npm install && gulp build:all"
  },
  "author": "Kevin Powell <kevinmpowell@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "esds-build": "^0.37.0",
    "esds-doc": "^0.14.0",
    "fs-extra": "^6.0.1",
    "gulp-tap": "^1.0.1",
    "slash": "^2.0.0"
  }
}

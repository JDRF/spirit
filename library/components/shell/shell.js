/**
 * Spirit Shell Scripts
 *
 * @return {void}
 */
const spiritShell = () => {
  const root = document.documentElement;

  if (root) {
    // Add html class for shell
    root.classList.add('spirit-shell');
  }
};

spiritShell();

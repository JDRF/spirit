{% macro tile_grid(
      class=false,
      tile_count=1) %}
      <div class="spirit-tile-grid{{ ' ' + class if class }}{{ ' spirit-tile-grid--carousel' if carousel }}">
        {{ caller() }}
      </div>
{% endmacro %}
{% extends "templates/sink.njk" %}
{% block body %}
    <link rel="stylesheet" href="/styles/typography-sink-page.css">
    {% filter markdown(true, "hostile-sink-reset") %}
        # Typography Sink
    {% endfilter %}
    <div class="spirit-container">
        <div class="spirit-row">
            <div class="spirit-col spirit-col--lg-6">

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Display 1
                {% endfilter %}
                <h1 class="spirit-display-1">You Can Help Turn Type One Into Type None</h1>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Display 2
                {% endfilter %}
                <h2 class="spirit-display-2">You Can Help Turn Type One Into Type None</h2>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h1
                {% endfilter %}
                <h1 class="spirit-h1">You Can Help Turn Type One Into Type None</h1>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h2
                {% endfilter %}
                <h2 class="spirit-h2">You Can Help Turn Type One Into Type None</h2>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h3
                {% endfilter %}
                <h3 class="spirit-h3">You Can Help Turn Type One Into Type None</h3>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h4
                {% endfilter %}
                <h4 class="spirit-h4">You Can Help Turn Type One Into Type None</h4>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h5
                {% endfilter %}
                <h5 class="spirit-h5">You Can Help Turn Type One Into Type None</h5>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h6
                {% endfilter %}
                <h6 class="spirit-h6">You Can Help Turn Type One Into Type None</h6>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Subhead
                {% endfilter %}
                <p class="spirit-subhead">JDRF is the leading global organization funding type 1 diabetes (T1D) research. Our strength lies in our exclusive focus and singular influence on the worldwide effort to end T1D.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Large
                {% endfilter %}
                <p class="spirit-body-text-l">Type 1 diabetes is an autoimmune disease that strikes both children and adults suddenly. It has nothing to do with diet or lifestyle. There is nothing you can do to prevent it. And, at present, there is no cure.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Medium
                {% endfilter %}
                <p class="spirit-body-text-m">Type 1 diabetes is an autoimmune disease that strikes both children and adults suddenly. It has nothing to do with diet or lifestyle. There is nothing you can do to prevent it. And, at present, there is no cure.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Small
                {% endfilter %}
                <p class="spirit-body-text-s">Boris P. Kovatchev, Ph.D.</p>

            </div>
            <div class="spirit-col spirit-col--lg-6 spirit-context--brand">

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Display 1
                {% endfilter %}
                <h1 class="spirit-display-1">You Can Help Turn Type One Into Type None</h1>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Display 2
                {% endfilter %}
                <h2 class="spirit-display-2">You Can Help Turn Type One Into Type None</h2>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h1
                {% endfilter %}
                <h1 class="spirit-h1">You Can Help Turn Type One Into Type None</h1>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h2
                {% endfilter %}
                <h2 class="spirit-h2">You Can Help Turn Type One Into Type None</h2>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h3
                {% endfilter %}
                <h3 class="spirit-h3">You Can Help Turn Type One Into Type None</h3>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h4
                {% endfilter %}
                <h4 class="spirit-h4">You Can Help Turn Type One Into Type None</h4>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h5
                {% endfilter %}
                <h5 class="spirit-h5">You Can Help Turn Type One Into Type None</h5>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### h6
                {% endfilter %}
                <h6 class="spirit-h6">You Can Help Turn Type One Into Type None</h6>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Subhead
                {% endfilter %}
                <p class="spirit-subhead">JDRF is the leading global organization funding type 1 diabetes (T1D) research. Our strength lies in our exclusive focus and singular influence on the worldwide effort to end T1D.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Large
                {% endfilter %}
                <p class="spirit-body-text-l">Type 1 diabetes is an autoimmune disease that strikes both children and adults suddenly. It has nothing to do with diet or lifestyle. There is nothing you can do to prevent it. And, at present, there is no cure.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Medium
                {% endfilter %}
                <p class="spirit-body-text-m">Type 1 diabetes is an autoimmune disease that strikes both children and adults suddenly. It has nothing to do with diet or lifestyle. There is nothing you can do to prevent it. And, at present, there is no cure.</p>

                {% filter markdown(true, "hostile-sink-reset") %}
                    ### Body Text Small
                {% endfilter %}
                <p class="spirit-body-text-s">Boris P. Kovatchev, Ph.D.</p>

            </div>
        </div>
    </div>
{% endblock body %}
